# MINI PROD runtime dependencies (servers + mini web settings).

# Web / ASGI
django>=5.2,<5.3
daphne>=4.1
channels>=4.1
channels-redis>=4.2
django-cors-headers>=4.4
django-auth-ldap>=5.0
python-ldap>=3.4

# Database (PostgreSQL)
psycopg[binary]>=3.1.8

# Server connections / encryption
asyncssh>=2.14
cryptography>=42.0

# Config / logging / schema
python-dotenv>=1.0
loguru>=0.7
pydantic>=2.8
PyYAML>=6.0


# LLM providers used by mini settings + terminal AI
google-genai>=1.0
httpx>=0.27
aiohttp>=3.10

# Claude provider
anthropic>=0.40.0

# Studio: scheduler for cron-based pipeline triggers
croniter>=3.0

# Task queue (memory ingestion, dream cycles)
# In dev without Redis: CELERY_TASK_ALWAYS_EAGER=True runs tasks synchronously
celery>=5.3
redis>=5.0

# Development, test and architecture tools intentionally live in
# requirements-dev.in / requirements-dev.lock. Production images install only
# this runtime set through requirements.lock.
