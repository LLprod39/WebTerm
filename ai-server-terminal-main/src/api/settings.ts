/**
 * src/api/settings.ts — Platform settings, models, activity.
 *
 * Migration target for functions from src/lib/api.ts related to:
 *   - LLM settings (fetchSettings, saveSettings, checkSettings)
 *   - Model list (fetchModels, refreshModels)
 *   - Activity logs (fetchActivityLogs)
 *   - Access management (users, groups, permissions)
 *   - SSO settings
 *
 * Status: re-exported from src/api/index.ts (pending migration)
 */
