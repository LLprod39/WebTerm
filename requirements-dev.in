# Canonical development and CI environment.
#
# This lock includes the production runtime plus every tool needed by the
# repository test, lint, coverage, architecture and pre-commit gates.
-r requirements-mini.txt
-c requirements.lock

pytest>=9,<10
pytest-django>=4.11,<5
pytest-asyncio>=1.3,<2
pytest-cov>=7,<8
coverage[toml]>=7.13,<8
ruff>=0.14,<0.15
import-linter>=2.13,<3
pre-commit>=4.5,<5
